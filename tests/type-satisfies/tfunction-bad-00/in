# β -> α !<: α -> β
#
# interface α {
# }
#
# interface β {
#   foo LITERAL
# }
#
# If someone wants a α -> β, then they want to be able to pass a α into
# the function.  The real function requires a β, though, so this doesn't
# succeed.  Additionally, they want to receive a β, but the real
# function only produces a α.  This also fails.

tliteral %0;
tinterface %1 = {};
tinterface %2 = {
  "foo": %0
};
tfunction %3 = %1 -> %2;
tfunction %4 = %2 -> %1;
tuple $0 = (%4,%3);
return $0;
